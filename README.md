Project setup

# Dependencies

```
yarn add axios react react-dom redux react-redux @reduxjs/toolkit semantic-ui-css semantic-ui-react styled-components react-router react-router-dom
```

# DevDependencies
```
yarn add -D @types/react @types/react-dom typescript @types/react-redux @types/styled-components @types/react-router-dom
```

# Webpack
```
yarn add -D webpack webpack-cli webpack-dev-server cross-env @types/webpack-dev-server @types/webpack @types/webpack-env @types/node html-webpack-plugin clean-webpack-plugin terser-webpack-plugin html-loader source-map-loader ts-loader webpack-merge
```

# Babel
```
yarn add -D @babel/core @babel/preset-env @babel/preset-react @babel/preset-typescript @babel/plugin-transform-runtime @babel/runtime babel-plugin-styled-components
```

# ESLint
```
yarn add -D eslint eslint-plugin-react eslint-plugin-react-hooks @typescript-eslint/parser @typescript-eslint/eslint-plugin eslint-plugin-compat
```

# Extra
```
yarn add -D uuid @types/uuid
```